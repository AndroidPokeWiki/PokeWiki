<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="65dp">

    <com.ruffian.library.widget.RView
        android:id="@+id/search_close_bg"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_marginStart="-10dp"
        android:layout_marginTop="-10dp"
        android:layout_marginEnd="-10dp"
        app:background_normal="@color/poke_ball_red"
        app:shadow_radius="10dp" />

    <ImageView
        android:id="@+id/search_close_icon"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginStart="19dp"
        android:layout_marginBottom="5dp"
        android:src="@drawable/logo"

        app:layout_constraintBottom_toBottomOf="@id/search_close_bg"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintWidth_percent="0.2556" />

    <com.ruffian.library.widget.RTextView
        android:id="@+id/search_close_btn"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:layout_marginStart="20dp"
        android:clickable="true"
        android:focusable="true"
        android:paddingStart="10dp"
        android:paddingEnd="10dp"

        app:background_normal="@color/white"
        app:corner_radius="100dp"
        app:icon_normal_right="@drawable/search_pokeball"
        app:layout_constraintBottom_toBottomOf="@id/search_close_icon"
        app:layout_constraintHeight_percent="0.5"
        app:layout_constraintStart_toEndOf="@id/search_close_icon"
        app:layout_constraintTop_toTopOf="@id/search_close_icon"
        app:layout_constraintWidth_percent="0.6" />

    <View
        android:id="@+id/search_close_mask"
        android:layout_width="match_parent"
        android:layout_height="match_parent"/>

</androidx.constraintlayout.widget.ConstraintLayout>